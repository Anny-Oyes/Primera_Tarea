// Calcular el perímetro de un cuadrado.

function perimetroCuadrado (longitudLado: number, valorfijo: number = 4) {
    return (valorfijo * longitudLado);
}
console.log("El resultado es: " + perimetroCuadrado(6) + " cm")